import React from "react";
import { Box, Typography } from "@mui/material";

export default function Settings() {
  return (
    <Box p={3}>
      <Typography variant="h4">Settings</Typography>
      <Typography mt={1}>Application settings and preferences.</Typography>
    </Box>
  );
}
